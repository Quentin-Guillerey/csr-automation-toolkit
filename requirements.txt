# Core (required)
requests==2.31.0

# Optional — only needed if you configure Google Sheets logging in Settings.
# The toolkit runs fine without these; CSV logging always works regardless.
gspread==5.12.0
oauth2client==4.1.3

# Optional — only needed if you configure an OpenAI API key in Settings.
openai==1.3.0
