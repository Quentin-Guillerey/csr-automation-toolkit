# Core (required)
requests==2.31.0

# Optional - only needed if you configure Google Sheets logging in Settings.
# The toolkit runs fine without this; CSV logging always works regardless.
# gspread handles service-account auth natively (gspread.service_account_from_dict),
# so oauth2client is no longer a dependency.
gspread==6.1.2

# Optional - only needed if you configure an OpenAI API key in Settings.
openai==1.51.0
