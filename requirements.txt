# CSR Automation Toolkit — dependencies
#
# The toolkit runs fully offline with the standard library plus requests
# (keyword matching + local CSV log). gspread and openai power optional
# features, but BOTH must be installed on the build machine when packaging
# with PyInstaller, or the .exe ships permanently missing those features.
#
# Removed in the 2026-07 patch:
#   oauth2client   (deprecated; replaced by gspread's native service-account auth)
#   python-dotenv  (replaced by the in-app setup screen and per-user config.json)

requests>=2.31

# Optional at runtime, required on the build machine:
gspread>=5.12          # pulls in google-auth
openai>=1.0
